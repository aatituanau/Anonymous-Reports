export function EmptyValue({children}) {
  return (
    <span className="border-b border-dotted border-outline-variant px-1 text-on-surface-variant">
      {children}
    </span>
  );
}

export default EmptyValue;
